import { BaseCollection, Client, Platform } from "../index.js";

exports.default = (async () => {
  // Collection
  const client = new BaseCollection();
  return client, { ...new BaseCollection() };
})();

exports.default = (async () => {
  // Platform
  const isValueString = String;
})();

function isObjectIncludesString(object = {}) {
  const isObject = new Object(object);
}
